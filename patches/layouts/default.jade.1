diff --git a/layouts/default.jade b/layouts/default.jade
index a642247..6be143c 100644
--- a/layouts/default.jade
+++ b/layouts/default.jade
@@ -7,5 +7,13 @@ body
     header
         h1 Cookbook
     main: article
+        - var availableLocales = locales.filter(x => x !== locale && !!lang(x));
+        if availableLocales
+        p Also available in#{' '}
+            each lc, index in availableLocales
+                a(href=`/${lang(lc).path}`)= lc
+                if index < availableLocales.length - 1
+                    |,#{' '}
+            |.
         h1= title
         != contents
